export enum EUserType {
  'Student',
  'Teacher',
  'Admin',
}

export enum UQuestionType {
  'True/False',
  'Quiz',
}

export enum UOptionQuestion {
  'Single',
  'Multiple',
}

export enum UPointType {
  'Standard',
  'Double',
  'BasedOnTime',
}
